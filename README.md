# docker-multi-hop-ssh

## Usage

```console
# generate public/private key
$ make ssh_keygen

# generate public/private key & docker-compose build
$ make build

# docker-compose up -d
$ make up

# multi-hop ssh
$ make ssh
```
